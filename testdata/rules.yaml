# 招投标自定义规则样例
# 用于 Step 11 NEW_ONLY 验收测试

rules:
  - rule_id: "MUST_HIT_001"
    name: "必命中规则-招标人存在性检查"
    description: "验证招标文件中必须包含'招标人'关键词"
    type: "exists"
    query: "招标人"
    dimension: "合规性"
    severity: "low"
    rigid: false
    remark: "此规则用于验证 RULES_MODE=NEW_ONLY 是否真正执行"

# 可选：自定义关键词
keywords: []

# 可选：风险检查项
risk_checks: []
