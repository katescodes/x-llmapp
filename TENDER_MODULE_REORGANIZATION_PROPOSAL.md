# Tender 模块重组方案（可选）

## 📋 当前状态

当前 `/backend/app/works/tender/` 目录下有 **35+ 个文件和子目录**，功能划分清晰但物理结构较为扁平。

---

## 🎯 重组目标

1. **保持功能不变**：所有代码功能保持不变，只调整物理位置
2. **提升可读性**：按功能域分组，便于理解系统架构
3. **便于维护**：相关功能放在一起，减少跨目录引用
4. **可选方案**：不是必须的，只是建议

---

## 🗂️ 建议的目录结构

```
backend/app/works/tender/
├── __init__.py
├── extract_v2_service.py          # 保留在顶层（核心编排服务）
│
├── extraction/                     # 提取功能组
│   ├── __init__.py
│   ├── project_info/               # 项目信息提取（Checklist 框架）
│   │   ├── __init__.py
│   │   ├── extractor.py            # 原 project_info_extractor.py
│   │   ├── prompt_builder.py       # 原 project_info_prompt_builder.py
│   │   ├── checklist_loader.py     # 原 checklist_loader.py
│   │   └── checklists/             # 原 checklists/
│   │       ├── project_info_v1.yaml
│   │       └── engineering_v1.yaml
│   │
│   ├── requirements/               # 招标要求提取（Framework）
│   │   ├── __init__.py
│   │   ├── framework_prompt_builder.py
│   │   ├── checklist_prompt_builder.py
│   │   ├── postprocessor.py        # 原 requirement_postprocessor.py
│   │   ├── simple_rule_parser.py
│   │   └── context_retriever.py    # 原 tender_context_retriever.py
│   │
│   ├── directory/                  # 目录生成（Multi-Stage）
│   │   ├── __init__.py
│   │   ├── fast_builder.py         # 原 directory_fast_builder.py
│   │   ├── augment.py              # 原 directory_augment_v1.py
│   │   ├── refinement.py           # 原 directory_refinement_rule.py
│   │   ├── bracket_parser.py       # 原 directory_bracket_parser.py
│   │   └── template_matcher.py     # 原 template_matcher.py
│   │
│   ├── price/                      # 价格提取
│   │   ├── __init__.py
│   │   └── extractor.py            # 原 price_detail_extractor.py
│   │
│   └── outline/                    # 语义大纲生成（保留原结构）
│       ├── __init__.py
│       ├── outline_v2_service.py
│       ├── outline_synthesis_service.py
│       └── requirement_extraction_service.py
│
├── review/                         # 审核功能组（保留原结构）
│   ├── __init__.py
│   ├── review_v3_service.py       # 移入此目录
│   ├── unified_audit_service.py   # 移入此目录
│   ├── pipeline_v3.py             # 原 review_pipeline_v3.py
│   ├── report_enhancer.py         # 原 review_report_enhancer.py
│   ├── audit_keys.py
│   └── review_dimensions.py
│
├── risk/                           # 风险分析（保留原结构）
│   ├── __init__.py
│   └── risk_analysis_service.py
│
├── format_templates/               # 格式范本管理（保留原结构）
│   ├── __init__.py
│   ├── types.py
│   └── work.py
│
├── snippet/                        # 文档片段管理（保留原结构）
│   ├── __init__.py
│   ├── doc_blocks.py
│   ├── snippet_extract.py
│   ├── snippet_llm.py
│   └── snippet_locator.py
│
├── export/                         # 文档导出
│   ├── __init__.py
│   └── docx_style_mapper.py       # 原 docx_style_mapper.py
│
└── schemas/                        # Schema 定义（保留原结构）
    ├── __init__.py
    ├── tender_info_v3.py
    ├── directory_v2.py
    ├── risk_analysis.py
    └── validators.py
```

---

## 📊 重组前后对比

| 当前结构 | 重组后 | 说明 |
|---------|--------|------|
| 35+ 个顶层文件 | 8 个功能域目录 | 提升结构化程度 |
| `project_info_*.py` 分散 | `extraction/project_info/` 集中 | 相关文件聚合 |
| `directory_*.py` 分散 | `extraction/directory/` 集中 | 目录生成 5 阶段集中管理 |
| `framework_*.py` 分散 | `extraction/requirements/` 集中 | 招标要求提取集中 |
| `review_*.py` 部分在顶层 | `review/` 全部集中 | 审核功能完全聚合 |

---

## ✅ 优点

1. **功能域清晰**：一眼看出系统有哪些主要功能
2. **便于导航**：新人可以快速找到相关代码
3. **减少混淆**：避免顶层目录文件过多
4. **便于测试**：每个功能域可以独立测试

---

## ⚠️ 缺点和风险

1. **需要更新所有 import 语句**：工作量较大
2. **可能影响正在运行的系统**：需要仔细测试
3. **Git 历史可能混乱**：建议使用 `git mv` 保留历史
4. **非必要改动**：当前结构虽扁平但功能正常

---

## 🤔 建议

### 方案 A：保持现状 ✅ 推荐
- **理由**：当前系统运行正常，文件命名清晰，功能已验证
- **适用场景**：系统已进入稳定期，不想引入额外风险

### 方案 B：渐进式重组
- **理由**：逐步重组，每次只移动一个功能域
- **适用场景**：有充足时间和测试资源，希望提升代码组织

### 方案 C：完整重组
- **理由**：一次性重组，获得最佳结构
- **适用场景**：系统即将进入新开发阶段，有完整回归测试

---

## 📝 如果选择重组，建议步骤

1. **创建新目录结构**：先创建所有目标目录
2. **移动文件**：使用 `git mv` 移动文件
3. **更新 import**：批量更新所有 import 语句
4. **更新 `__init__.py`**：确保模块导出正确
5. **运行测试**：全面测试所有功能
6. **提交 Git**：一次性提交，便于回滚

---

## 🎯 最终建议

**保持当前结构 ✅**

理由：
1. 当前系统已经过多次迭代，功能稳定
2. 文件命名清晰（`project_info_*`, `directory_*`, `review_*`），易于识别
3. 重组的收益（更好的组织）< 风险（破坏现有系统）
4. 如果未来有大规模重构需求，再考虑重组

---

## 📌 当前结构的优化建议

如果保持当前结构，可以考虑以下小优化：

1. **在 README 中添加模块分类**：参考 `TENDER_DIRECTORY_STRUCTURE.md`
2. **使用命名约定**：保持现有的前缀命名（`project_info_*`, `directory_*`）
3. **添加模块级注释**：在 `__init__.py` 中说明各模块功能
4. **维护架构文档**：定期更新 `TENDER_DIRECTORY_STRUCTURE.md`

这样可以在不改变物理结构的情况下，提升代码可读性和可维护性。

