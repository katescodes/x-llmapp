# ✨ V3 增量展示机制详解

## 🎯 核心问题

> "是不是抽取一类信息就展示一类信息？"

## ✅ 答案：是的！

V3 系统采用 **增量抽取 + 增量展示** 的机制，**每完成一个类别的抽取，前端就会立即显示该类别的数据**。

---

## 🔄 完整流程图

```
用户点击"抽取项目信息"
        ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
后端：9 阶段顺序执行
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stage 1: 项目概况 (project_overview)
  ├─ LLM 调用 → 返回 project_overview 数据
  ├─ ✅ 写入数据库（增量更新）
  │   {
  │     "schema_version": "tender_info_v3",
  │     "project_overview": { ... },  ← 已有数据
  │     "scope_and_lots": {},          ← 空
  │     "schedule_and_submission": {}, ← 空
  │     ...
  │   }
  └─ 更新 run.progress = 0.15 (15%)
        ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
前端：轮询检测（每 2 秒）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

轮询检查 run.status
  ├─ status === 'running'
  ├─ 📥 GET /api/projects/{id}/project-info
  └─ setProjectInfo(data)
        ↓
React 重新渲染
  └─ ProjectInfoV3View 组件
        ↓
显示：
  ┌──────────────────────────────────────────┐
  │ 1️⃣ 项目概况 ✅                           │
  │   - project_name: "某某项目"              │
  │   - owner_name: "某某公司"                │
  │   - budget: "100万元"                     │
  │   📎 查看证据 (5)                         │
  └──────────────────────────────────────────┘
  
  （其他 8 个类别还未显示，因为数据为空）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stage 2: 范围与标段 (scope_and_lots)
  ├─ LLM 调用 → 返回 scope_and_lots 数据
  ├─ ✅ 写入数据库（增量更新）
  │   {
  │     "schema_version": "tender_info_v3",
  │     "project_overview": { ... },  ← 已有
  │     "scope_and_lots": { ... },    ← 新增
  │     "schedule_and_submission": {}, ← 空
  │     ...
  │   }
  └─ 更新 run.progress = 0.25 (25%)
        ↓
前端轮询检测
  ├─ 📥 GET /api/projects/{id}/project-info
  └─ setProjectInfo(data)
        ↓
显示：
  ┌──────────────────────────────────────────┐
  │ 1️⃣ 项目概况 ✅                           │
  │   - project_name: "某某项目"              │
  │   ...                                     │
  └──────────────────────────────────────────┘
  
  ┌──────────────────────────────────────────┐
  │ 2️⃣ 范围与标段 ✅ 新增！                  │
  │   - project_scope: "软件开发"            │
  │   - lot_division: "单一标段"             │
  │   📎 查看证据 (3)                         │
  └──────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stage 3: 进度与递交 (schedule_and_submission)
  ├─ LLM 调用 → 返回 schedule_and_submission 数据
  ├─ ✅ 写入数据库（增量更新）
  └─ 更新 run.progress = 0.35 (35%)
        ↓
前端轮询 → 显示 3 个类别
  ┌──────────────────────────────────────────┐
  │ 1️⃣ 项目概况 ✅                           │
  │ 2️⃣ 范围与标段 ✅                         │
  │ 3️⃣ 进度与递交 ✅ 新增！                  │
  │   - bid_deadline: "2024-12-31 14:00"     │
  │   - bid_opening_time: "2024-12-31 14:30" │
  │   ...                                     │
  └──────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

... 依次类推 ...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stage 9: 投标保证金 (bid_security)
  ├─ LLM 调用 → 返回 bid_security 数据
  ├─ ✅ 写入数据库（增量更新）
  └─ 更新 run.progress = 0.95 (95%)
        ↓
前端轮询 → 显示 9 个类别（全部完成）
  ┌──────────────────────────────────────────┐
  │ 1️⃣ 项目概况 ✅                           │
  │ 2️⃣ 范围与标段 ✅                         │
  │ 3️⃣ 进度与递交 ✅                         │
  │ 4️⃣ 投标人资格 ✅                         │
  │ 5️⃣ 评审与评分 ✅                         │
  │ 6️⃣ 商务条款 ✅                           │
  │ 7️⃣ 技术要求 ✅                           │
  │ 8️⃣ 文件编制 ✅                           │
  │ 9️⃣ 投标保证金 ✅ 新增！                  │
  │   - bid_bond_amount: "1万元"             │
  │   - bid_bond_form: "银行转账"            │
  │   ...                                     │
  └──────────────────────────────────────────┘
        ↓
run.status = 'success'
  ├─ 停止轮询
  └─ 显示"抽取完成"提示
```

---

## 🔑 关键技术点

### 1️⃣ **后端增量写入** (`extract_v2_service.py`)

```python
# 每完成一个 Stage 就立即写入数据库
for stage_num in range(1, 10):
    # ... LLM 调用 ...
    
    # ✅ 增量更新数据库
    incremental_data = {
        "schema_version": "tender_info_v3",
        **{k: stage_results.get(k, {}) for k in [s["key"] for s in stages]}
    }
    self.dao.upsert_project_info(
        project_id, 
        data_json=incremental_data,  # 包含已完成的所有 Stage
        evidence_chunk_ids=list(all_evidence_chunk_ids)
    )
    
    # ✅ 更新进度
    progress = 0.05 + stage_num * 0.1  # Stage 1: 15%, Stage 2: 25%, ..., Stage 9: 95%
    self.dao.update_run(run_id, "running", progress=progress, message=f"{stage_name}已完成")
```

**关键特性：**
- ✅ **每个 Stage 完成后立即写入**（不等所有 Stage 完成）
- ✅ **写入的是累积数据**（包含之前所有已完成的 Stage）
- ✅ **未完成的 Stage 为空对象** `{}`

---

### 2️⃣ **前端增量轮询** (`TenderWorkspace.tsx`)

```tsx
// 每 2 秒轮询一次 run 状态
const check = async () => {
  const run = await api.get(`/api/apps/tender/runs/${runId}`);
  
  if (run.status === 'running') {
    // ✅ 运行中：增量加载项目信息
    if (run.kind === 'extract_project_info') {
      const data = await api.get(`/api/apps/tender/projects/${projectId}/project-info`);
      setProjectInfo(data);  // 触发 React 重新渲染
    }
  }
};

setInterval(check, 2000);  // 每 2 秒执行一次
```

**关键特性：**
- ✅ **每 2 秒请求一次数据**
- ✅ **获取到最新的累积数据**
- ✅ **触发 React 重新渲染**

---

### 3️⃣ **前端智能渲染** (`ProjectInfoV3View.tsx`)

```tsx
// 遍历九大类
{TENDER_INFO_V3_CATEGORIES.map((categoryKey) => {
  const categoryData = tenderInfoV3[categoryKey];
  return renderV3Category(categoryKey, categoryData, onEvidence);
})}

// 渲染单个类别
const renderV3Category = (categoryKey, categoryData, onEvidence) => {
  // ✅ 如果数据为空对象，不渲染
  if (!categoryData || categoryKey === 'schema_version') return null;
  
  const fields = Object.entries(categoryData).filter(
    ([key]) => key !== 'evidence_chunk_ids'
  );
  
  // ✅ 如果没有字段，不渲染
  if (fields.length === 0) return null;
  
  // ✅ 有数据才渲染卡片
  return <div className="source-card">...</div>;
};
```

**关键特性：**
- ✅ **空对象不渲染**（`if (fields.length === 0) return null`）
- ✅ **有数据才显示卡片**
- ✅ **自动适配部分数据**（显示已完成的类别）

---

## 📊 数据示例

### Stage 1 完成后的数据库状态

```json
{
  "schema_version": "tender_info_v3",
  "project_overview": {
    "project_name": "某某项目",
    "project_number": "2024-001",
    "owner_name": "某某公司",
    "budget": "100万元",
    "evidence_chunk_ids": ["chunk_001", "chunk_002"]
  },
  "scope_and_lots": {},          ← 空对象
  "schedule_and_submission": {}, ← 空对象
  "bidder_qualification": {},    ← 空对象
  "evaluation_and_scoring": {},  ← 空对象
  "business_terms": {},          ← 空对象
  "technical_requirements": {}, ← 空对象
  "document_preparation": {},    ← 空对象
  "bid_security": {}             ← 空对象
}
```

**前端渲染结果：** 只显示 1 个卡片（`project_overview`）

---

### Stage 5 完成后的数据库状态

```json
{
  "schema_version": "tender_info_v3",
  "project_overview": { ... },       ← 有数据
  "scope_and_lots": { ... },         ← 有数据
  "schedule_and_submission": { ... }, ← 有数据
  "bidder_qualification": { ... },   ← 有数据
  "evaluation_and_scoring": { ... }, ← 有数据
  "business_terms": {},              ← 空对象
  "technical_requirements": {},     ← 空对象
  "document_preparation": {},        ← 空对象
  "bid_security": {}                 ← 空对象
}
```

**前端渲染结果：** 显示 5 个卡片（前 5 个类别）

---

## ⏱️ 时间线示例

| 时间 | 后端状态 | 前端显示 | 进度 |
|------|---------|---------|------|
| 0s | 任务开始 | 清空旧数据 | 5% |
| 10s | Stage 1 完成 | 显示 1 个类别 | 15% |
| 20s | Stage 2 完成 | 显示 2 个类别 | 25% |
| 30s | Stage 3 完成 | 显示 3 个类别 | 35% |
| 40s | Stage 4 完成 | 显示 4 个类别 | 45% |
| 50s | Stage 5 完成 | 显示 5 个类别 | 55% |
| 60s | Stage 6 完成 | 显示 6 个类别 | 65% |
| 70s | Stage 7 完成 | 显示 7 个类别 | 75% |
| 80s | Stage 8 完成 | 显示 8 个类别 | 85% |
| 90s | Stage 9 完成 | 显示 9 个类别 | 95% |
| 95s | 任务成功 | 停止轮询 | 100% |

---

## 🎯 用户体验优势

### ✅ **实时反馈**
- 用户不需要等待 90 秒才看到结果
- 每 10 秒就能看到新的类别信息

### ✅ **进度可视化**
- 可以看到数据逐步填充
- 进度条实时更新（15% → 25% → ... → 95%）

### ✅ **容错性强**
- 即使某个 Stage 失败，之前的数据仍然可见
- 用户可以看到部分结果，而不是完全失败

### ✅ **响应式设计**
- 自动适配部分数据
- 不需要等所有数据都完成才渲染

---

## 🔍 验证方法

### 方法 1：观察页面变化

1. 打开招投标工作台
2. 点击"抽取项目信息"
3. 观察页面（不要刷新）
4. 你会看到：
   - ⏱️ 0-10s：页面为空或显示 loading
   - ⏱️ 10s：出现第 1 个类别卡片
   - ⏱️ 20s：出现第 2 个类别卡片
   - ...
   - ⏱️ 90s：出现第 9 个类别卡片

### 方法 2：查看 Network 请求

1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 点击"抽取项目信息"
4. 你会看到：
   - 每 2 秒一个 `GET /api/apps/tender/projects/{id}/project-info` 请求
   - 每次请求返回的数据逐渐增多

### 方法 3：查看后端日志

```bash
# 观察后端日志
tail -f backend/logs/app.log | grep "ExtractV2: Stage"

# 你会看到：
# ExtractV2: Stage 1/9 done - data_keys=['project_name', 'project_number', ...]
# ExtractV2: Stage 1/9 incremental update done
# ExtractV2: Stage 2/9 done - data_keys=['project_scope', 'lot_division', ...]
# ExtractV2: Stage 2/9 incremental update done
# ...
```

---

## 🎉 总结

**是的！V3 系统采用增量抽取 + 增量展示机制：**

1. ✅ **后端**：每完成一个类别就写入数据库
2. ✅ **前端**：每 2 秒轮询一次，获取最新数据
3. ✅ **组件**：智能渲染，只显示有数据的类别
4. ✅ **结果**：用户可以实时看到数据逐步填充

**用户体验：**
- 不需要等待 90 秒才看到结果
- 每 10 秒就能看到新的类别信息
- 提供实时进度反馈
- 容错性强，部分数据仍可见

**这是一个完全自动化、实时响应的增量展示系统！** 🚀

