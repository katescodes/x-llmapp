# ==========================================
# 亿林亿问 Frontend 环境变量配置示例
# ==========================================

# ----- 开发服务器配置 -----
# 前端开发服务器端口
VITE_DEV_SERVER_PORT=6173

# 后端 API 配置（开发环境代理目标）
VITE_BACKEND_PROTOCOL=http
VITE_BACKEND_HOST=127.0.0.1
VITE_BACKEND_PORT=9001

# 是否禁用开发代理（生产环境设置为 true）
# VITE_DISABLE_DEV_PROXY=false

# ----- 公共访问配置（可选，用于外部访问） -----
# 用于从外部访问开发服务器（如容器化环境）
# VITE_DEV_SERVER_PUBLIC_HOST=your-domain.com
# VITE_DEV_SERVER_PUBLIC_PORT=443
# VITE_DEV_SERVER_PUBLIC_PROTOCOL=https

# ----- HMR（热模块替换）配置（可选） -----
# VITE_DEV_SERVER_HMR_HOST=
# VITE_DEV_SERVER_HMR_PORT=
# VITE_DEV_SERVER_HMR_PROTOCOL=
# VITE_DEV_SERVER_HMR_CLIENT_PORT=

# ==========================================
# Debug 功能配置
# ==========================================

# 是否显示 Debug 面板（仅开发环境使用）
VITE_SHOW_DEBUG=false

# API 基础 URL（如果需要直接调用 API 而不通过代理）
# VITE_API_BASE_URL=http://localhost:9001

